/** Authoritative Arcade body radii shared by entities, data validation, and
 * inside-edge spawn sampling. Presentation art must never redefine these. */
export const PLAYER_BODY_RADIUS = 14;
export const ENEMY_BODY_RADIUS = 13;
